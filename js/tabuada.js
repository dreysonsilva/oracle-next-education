document.write("<h1>TABUADA JS</h1>");

var numeroInformado = parseInt(prompt("Informe o numero"));

for(cont=1;cont<=10;cont++){
    document.write(numeroInformado +" X "+cont+" = "+(numero*cont)+"<br>");
}